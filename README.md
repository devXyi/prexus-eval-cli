# prexus-eval-cli
Prexus Eval is a local-first, multi-provider LLM evaluation CLI for AI models and agents. Build golden test sets, run LLM-as-a-judge evaluations, track regressions, analyze failure modes, and generate reproducible reports. Secure API keys are stored in the OS keychain, with Git-friendly local workspaces.
