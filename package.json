{
  "name": "prexus-eval",
  "version": "0.1.0",
  "description": "Local-first, multi-provider LLM eval CLI — golden test sets, LLM-as-judge scoring, regression tracking, failure-mode taxonomies. Real OS keychain for keys, real files for the workspace, no browser CORS limitations.",
  "bin": {
    "prexus-eval": "./bin/prexus-eval.js"
  },
  "main": "src/index.js",
  "engines": {
    "node": ">=18.0.0"
  },
  "dependencies": {
    "@napi-rs/keyring": "^1.3.0"
  },
  "license": "MIT"
}
